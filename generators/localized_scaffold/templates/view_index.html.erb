<h1><%%= t('<%= file_name %>.index.title') %></h1>

<table class="wide">
  <tr>
  <% for attribute in attributes -%>
    <%- next if has_parent? and attribute.name == "#{parent.file_name}_id" -%>
    <th><%%= t('activerecord.attributes.<%= file_name %>.<%= attribute.name %>') %></th>
  <% end -%>
    <th>&nbsp;</th>
  </tr>

  <%% @<%= plural_name %>.each do |<%= singular_name %>| %>
    <tr>
    <% for attribute in attributes -%>
      <%- next if has_parent? and attribute.name == "#{parent.file_name}_id" -%>
      <td><%%=h <%= singular_name %>.<%= attribute.name %> %></td>
    <% end -%>

      <td class="last">
        <%%= link_to t('standard.cmds.show'), <%= path_of_with_parent_if_any(:show, "#{singular_name}") %> %> |
        <%%= link_to t('standard.cmds.edit'), <%= path_of_with_parent_if_any(:edit, "#{singular_name}") %> %> |
        <%%= link_to t('standard.cmds.destroy'), <%= path_of_with_parent_if_any(:show, "#{singular_name}") %>,
          :confirm => t('standard.labels.are_you_sure'), :method => :delete %>
      </td>
    </tr>
  <%% end %>
</table>

<p>
<%- if has_will_paginate? -%>
  <%%= pagination @<%= table_name %> %>

<%- end -%>
  <%%= link_to t('<%= file_name %>.cmds.new'), <%= path_of_with_parent_if_any(:new) %> %>

  <span class="decent">
    <%%= t('standard.labels.or') %>
    <%%= link_to t('standard.cmds.home'), defined?(home_path) ? home_path : '/' %>
  </span>
</p>
